
import Button from '../core/Button';
import Form from './Form';

const Profile = () => {
  return <div className="header__profile">
    <Button className="header__profile-btn">Смотреть 14 дней бесплатно</Button>
    <Form />
  </div>
}

export default Profile;